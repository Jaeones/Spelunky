# GAME_PRODUCTION_PACKAGE

## 1. 게임 개요

프로젝트 목표:
- 현재 Unity 기반 Spelunky 스타일 저장소를 바탕으로
- `15-30분 플레이 가능한 4스테이지 완성형 소형 게임`을 제작한다.

제품 정의:
- 엔진: `Unity 2022.3.62f3`
- 장르: 2D 탐험 액션 플랫폼 게임
- 구조: 1회 런 기반 4스테이지 캠페인
- 목표 완주 시간:
  - 초회 완주: `18-26분`
  - 숙련 완주: `15-20분`

핵심 전제:
- 지금 저장소는 `프로토타입용 시스템 베이스`로는 강하다.
- 다만 Stage 흐름, Run 상태, UI 흐름, 콘텐츠 품질 통제가 아직 제품 수준은 아니다.
- 따라서 목표는 새 게임을 다시 만드는 것이 아니라, `기존 강점을 유지하면서 제품 구조를 덧입히는 것`이다.

---

## 2. 핵심 재미

반드시 유지할 재미:
- 정밀한 좌우 이동과 점프
- 짧은 점프, 낙하, 착지의 손맛
- 로프와 폭탄을 언제 쓸지 판단하는 긴장
- 작은 보상을 위해 위험한 경로를 선택하는 탐험 감각
- 적을 많이 상대하기보다 `지형 + 적 + 함정`을 읽는 플레이
- 보스 없이도 마지막 탈출이 클라이맥스가 되는 구조

디자인 원칙:
- 이동 감각은 초반에 크게 건드리지 않는다.
- 난이도는 `피해량`보다 `배치`, `가시성`, `자원 위치`, `보상 유혹`으로 만든다.
- 같은 적도 어느 Stage, 어느 지형, 어느 자원 상태에서 만나느냐에 따라 다른 판단을 요구해야 한다.

---

## 3. 4스테이지 설계

### Stage 1. Entry Mine

역할:
- 학습과 신뢰 형성

목표 시간:
- `3-5분`

핵심 요소:
- 넓은 발판
- Snake, Bat
- ArrowTrap, 짧은 낙하
- 눈에 보이는 소형 보상 루트

학습 포인트:
- 멈추고 읽기
- 짧은 점프
- 밟기와 채찍 거리
- 첫 자원 사용 판단

### Stage 2. Hanging Caverns

역할:
- 수직 탐험과 로프 압박

목표 시간:
- `4-6분`

핵심 요소:
- 긴 하강 루트
- Bat, Spider, 소수 Snake
- one-way platform 오독
- 상단/하단 보상 분기

학습 포인트:
- 아래 보기
- 착지 후 대응
- 로프를 보험처럼 쓰기

### Stage 3. Ruined Works

역할:
- 정밀 돌파와 폭탄 판단

목표 시간:
- `4-7분`

핵심 요소:
- 협로
- Caveman, Spider, Bat
- ArrowTrap, MovingPlatform, Block
- 폭탄 지름길과 퇴로 리스크

학습 포인트:
- 폭탄은 지형 해결 도구
- 다음 방 진입 전 스캔
- 욕심과 안전의 균형

### Stage 4. Idol Vault Escape

역할:
- 결산과 탈출 클라이맥스

목표 시간:
- `5-8분`

핵심 요소:
- 유물 회수 전 탐험
- 유물 회수 후 탈출 압박
- Bat, Spider, Caveman 혼합 배치
- ArrowTrap, 재추락 리스크, 최종 관문

학습 포인트:
- 자원 결산
- 마지막 판단
- 익숙한 위협을 긴장 상태에서 다시 처리하기

---

## 4. 플레이타임 모델

전체 목표:
- 총 `16-26분`을 중심으로 조정
- 상한은 `30분` 이내 유지

권장 분배:
- Stage 1: `3-5분`
- Stage 2: `4-6분`
- Stage 3: `4-7분`
- Stage 4: `5-8분`

실패 분포 목표:
- 초회 플레이어는 `Stage 2-3`에서 가장 많이 실패
- 숙련 플레이어는 `Stage 4`만 긴장감이 남아야 함

리듬 목표:
- `30-60초마다` 의미 있는 판단 1회
- `2-3분마다` 자원 사용을 고민하는 큰 갈림길 1회
- 각 Stage 종료 전 `핵심 학습을 다시 시험하는 관문` 1회

---

## 5. 시스템 구조

### 씬 구조

권장 씬:
- `Boot`
- `Menu`
- `Game`
- `Result`

권장 운영 방식:
- 실제 플레이는 `Game` 씬 하나를 유지
- Stage 전환은 씬 교체가 아니라 `Stage 데이터 교체`로 처리
- Transition은 별도 씬보다 `Game` 씬 오버레이 UI 권장

### 런타임 매니저 구조

필수 권장 매니저:
- `GameLoopManager`
- `RunManager`
- `StageManager`
- `SpawnManager`
- `UIManager`
- `AudioManager`
- `DebugManager`

현재 저장소에서 재사용 가치가 높은 영역:
- `EntityPhysics`
- `PhysicsBody`
- `GameManager`의 틱 순서 구조
- `EntityManager`, `PlatformManager`, `TimerManager`
- `Player`, `Bomb`, `Rope`, `Enemy`, `ArrowTrap`
- `LevelGenerator`, `Room`, `Tile`

가장 먼저 분리해야 할 현재 병목:
- `PlayerEnterDoorState`의 직접 씬 리로드
- `GameManager`의 과도한 책임 집중
- `PlayerUI`의 씬 이름 의존성

---

## 6. 구현 우선순위

### 1차 목표: 러프 완주 버전

목표:
- 시작부터 Stage 4까지 한 번에 진행 가능
- Stage 전환, 자원 유지, 엔딩까지 연결

필수 작업:
- `RunState` 최소 구조
- `StageFlow` 진입점
- 4개 Stage의 러프 메인 루트
- 출구 진입 -> 다음 Stage 흐름
- Stage 4 임시 클라이맥스

### 2차 목표: 적/함정/자원 완성

목표:
- 각 Stage가 실제로 다르게 느껴짐
- 자원 판단이 살아남

필수 작업:
- 적/함정 풀 Stage별 확정
- 보상 루트와 지름길 설계
- Bomb / Rope 경제 연결
- 유물 회수 후 탈출 압박 강화

### 3차 목표: 밸런스/연출/피드백

목표:
- 플레이타임과 난이도 곡선 정리
- 반복 플레이에서도 긴장 유지

필수 작업:
- 플레이타임 조정
- UI/오디오/피격 피드백 보강
- 억울한 죽음 제거
- 재시도 흐름 정리

---

## 7. 밸런스 초안

현재 구현 기준선:
- 체력 `4`
- 시작 폭탄 `4`
- 시작 로프 `4`
- `runSpeed = 48`
- `sprintSpeed = 96`
- `maxJumpHeight = 24`
- 적 접촉 피해 `1`
- ArrowTrap 체감 피해 `2칸급`
- Bomb explosion `즉사급`

Stage별 자원 압박:
- Stage 1: 사용법 학습
- Stage 2: 로프 압박
- Stage 3: 폭탄 압박
- Stage 4: 남은 전체 자원 결산

전체 런 자원 총량 목표:
- 폭탄 접근량: `8-12`
- 로프 접근량: `7-11`

보상 비율:
- Stage 1: 안전 `60`, 위험 `30`, 고위험 `10`
- Stage 2: 안전 `50`, 위험 `35`, 고위험 `15`
- Stage 3: 안전 `40`, 위험 `40`, 고위험 `20`
- Stage 4: 안전 `35`, 위험 `35`, 고위험 `30`

튜닝 원칙:
- 피해량보다 배치와 경제를 먼저 조정
- 조작감 핵심 수치는 마지막에만 건드림

---

## 8. QA 체크리스트

필수 시스템 QA:
- Stage 전환 시 플레이어 중복 생성 없음
- 카메라가 항상 현재 플레이어를 추적함
- RunState가 스테이지 사이에서 유지됨
- 출구 진입 후 입력 잠금과 전환 흐름이 정상
- 사망 후 재시작 시 상태가 꼬이지 않음
- Stage 4 클라이맥스가 항상 정상 종료 가능

물리 QA:
- one-way platform 통과/착지 안정성
- moving platform 탑승/하차 안정성
- 폭탄 폭발과 타일 제거 후 소프트락 여부
- 낙하 후 착지 판정 안정성

콘텐츠 QA:
- 입구 직후 무회피 피격 없음
- 출구 접근 불가 생성 없음
- Key/Chest/GoldIdol 이벤트 누락 없음
- Stage 4 탈출 경로 항상 존재

---

## 9. 플레이테스트 체크리스트

테스트 후 반드시 물을 질문:
- Stage 1을 자주 넘길 수 있었는가?
- Stage 2에서 로프를 아끼거나 써야 한다는 고민이 생겼는가?
- Stage 3에서 폭탄이 실제로 아까웠는가?
- Stage 4에서 남겨온 자원이 의미 있었는가?
- 죽었을 때 억울했는가, 아니면 내가 욕심냈다고 느꼈는가?
- 보상이 위험을 감수할 만큼 매력적이었는가?
- 너무 길거나 반복적으로 느껴지는 구간이 있었는가?
- 마지막 클라이맥스가 보스 없이도 기억에 남았는가?

수집하면 좋은 데이터:
- 도달 스테이지
- 사망 원인
- Stage별 체류 시간
- 폭탄/로프 사용량
- 유물 회수율
- Stage 4 탈출 성공률

---

## 10. 위험요소 및 대응책

### 위험 1. `GameManager` 과부하

문제:
- 게임 루프, 스폰, 게임오버, 레벨 초기화가 한곳에 몰려 있음

대응:
- `GameLoopManager`, `RunManager`, `StageManager`로 책임 분리
- 단, 한 번에 갈아엎지 말고 진입점부터 분리

### 위험 2. Stage 전환 구조 부재

문제:
- 현재 출구 진입이 씬 리로드 기반

대응:
- `PlayerEnterDoorState`가 직접 씬을 다시 로드하지 않게 변경
- 임시로 GameManager 콜백 -> 최종적으로 StageManager 전환

### 위험 3. 룸 생성 품질 불안정

문제:
- 현재 `LevelGenerator`는 WIP 성격이 강함

대응:
- 처음엔 완전 procedural보다 `핵심 룸 풀 + 제한된 조합` 사용
- hand-authored 메인 루트 중심으로 시작

### 위험 4. UI 결합도 과다

문제:
- `PlayerUI`의 `GameObject.Find()` 의존성

대응:
- 인스펙터 기반 참조 또는 UIManager 바인딩 구조로 전환

### 위험 5. Stage 4 클라이맥스 구현 난이도

문제:
- 보스 없이 강한 결말을 만들려면 시스템 연동이 필요함

대응:
- 유물 회수 -> 탈출 타이머/오디오/UI 전환의 단순 구조부터 구현
- 보스전은 도입하지 않음

### 위험 6. 밸런스 조정 방향 상실

문제:
- 어렵다고 느끼면 피해량부터 건드릴 위험

대응:
- 조정 우선순위를 고정
- `길이 -> 자원 위치 -> 위협 조합 -> 빈도 -> 가시성 -> 보상 -> 조작감 -> 피해량`

---

## 최종 결론

이 프로젝트는 이미 `이동`, `점프`, `로프`, `폭탄`, `탐험`, `위험 회피`라는 핵심 재미를 담을 시스템 기반을 갖고 있다.
따라서 성공의 핵심은 새로운 시스템을 많이 만드는 것이 아니라,
아래 순서를 지키는 것이다.

`Run/Stage 구조 정리 -> 4스테이지 러프 완주 연결 -> 적/함정/자원 밀도 강화 -> Stage 4 클라이맥스 완성 -> 밸런스와 피드백 조정`

이 순서를 지키면,
현재 저장소는 학습용 최소 복제가 아니라 `실제로 완주 가능한 15-30분짜리 4스테이지 게임`으로 확장할 수 있다.
