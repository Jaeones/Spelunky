# WEEK1_ACTIONS

## 목적

이 문서는 현재 저장소를 `15-30분 플레이 가능한 4스테이지 게임`으로 확장하기 위한
`1주차 작업 목록`이다.

기준 역할:
- Unity 시니어 개발자 관점: 구조를 먼저 세우고, 나중에 콘텐츠를 올린다.
- 게임 디렉터 관점: 플레이타임과 난이도 곡선을 망치지 않는 가장 작은 시작점을 선택한다.

핵심 원칙:
- 한 번에 크게 갈아엎지 않는다.
- 먼저 `끝까지 연결될 수 있는 구조`를 만든다.
- 콘텐츠보다 `흐름`, `상태`, `전환`을 먼저 정리한다.

---

## 1. 현재 저장소 기준으로 가장 먼저 손대야 할 파일

### 1. `Assets/Scripts/Player/States/PlayerEnterDoorState.cs`

왜 먼저 손대야 하는가:
- 현재는 출구 진입 시 `현재 씬을 다시 로드`한다.
- 이 구조로는 4스테이지 진행, 런 상태 유지, Stage 전환이 불가능하다.
- 4스테이지 게임 확장의 첫 관문이다.

현재 문제:
- `SceneManager.LoadScene(scene.name)` 임시 구현
- 체력, 폭탄, 로프, 골드, 액세서리를 다음 스테이지로 넘길 수 없음

1주차 목표:
- 씬 리로드 대신 `GameManager` 또는 임시 `Run/Stage 전환 진입점`을 호출하도록 바꾸는 방향 설계

플레이타임/난이도 영향:
- 직접 난이도를 바꾸진 않지만,
  4스테이지 구조가 가능해지므로 전체 플레이타임 설계의 시작점이 된다.

### 2. `Assets/Scripts/Misc/GameManager.cs`

왜 먼저 손대야 하는가:
- 현재 이 클래스가 게임 루프, 레벨 초기화, 플레이어 스폰, 게임오버까지 모두 맡고 있다.
- 지금은 프로토타입으로는 괜찮지만, 4스테이지 제품 구조에선 책임이 너무 많다.

현재 문제:
- 런 상태 개념 없음
- 스테이지 전환 개념 없음
- 플레이어 스폰과 레벨 생성이 강하게 결합됨

1주차 목표:
- 기존 게임 루프는 유지하면서,
  `런 시작`, `스테이지 시작`, `플레이어 스폰` 진입점을 더 명확히 분리할 준비를 한다.

플레이타임/난이도 영향:
- 직접 수치에는 영향 없음
- 하지만 이후 콘텐츠 작업 속도와 버그율에 큰 영향을 준다.

### 3. `Assets/Scripts/LevelGenerator/LevelGenerator.cs`

왜 먼저 손대야 하는가:
- Stage별 룸 풀, 입구/출구, 스테이지 정체성을 데이터로 나누려면 여기가 중심이다.
- 현재도 재사용 가치는 높지만, 아직 `single level WIP` 가정이 강하다.

현재 문제:
- 4개의 Stage 구분이 없음
- Stage별 룸 풀과 스폰 규칙을 주입하는 구조가 없음

1주차 목표:
- Stage 데이터를 받아 룸 풀을 바꿔 끼울 수 있는 방향을 설계
- 당장은 완전한 데이터 기반이 아니어도 좋고, `Stage 1/2/3/4용 룸 배열 분기점`만 있어도 충분

플레이타임/난이도 영향:
- Stage 길이와 위협 구조의 차별화를 가능하게 한다.

### 4. `Assets/Scripts/Player/PlayerUI.cs`

왜 먼저 손대야 하는가:
- 현재 `GameObject.Find()` 기반이라 유지보수와 씬 구조 확장에 취약하다.
- Boot/Menu/Game/Result 구조로 가면 가장 먼저 깨질 가능성이 높은 UI다.

현재 문제:
- 씬 오브젝트 이름 의존성 강함
- HUD가 플레이어에 강하게 붙어 있어 결과/전환 UI 확장에 불리함

1주차 목표:
- 당장은 전체 UIManager까지 안 가도,
  최소한 레퍼런스를 인스펙터 기반 또는 바인딩 기반으로 옮길 준비를 한다.

플레이타임/난이도 영향:
- 직접 난이도 변화 없음
- 하지만 플레이어가 체력/폭탄/로프를 정확히 읽는 것은 실제 체감 난이도에 크게 영향 준다.

### 5. `Assets/Scripts/UI/GameOverUI.cs`

왜 먼저 손대야 하는가:
- 현재는 `게임오버 -> 현재 씬 재로드` 구조다.
- Result 화면, Retry 정책, Menu 복귀, RunResult 저장으로 확장해야 한다.

현재 문제:
- 제품 구조의 결과 화면이 아니라 임시 재시작 화면에 가깝다.

1주차 목표:
- UI를 크게 갈아엎기보다,
  `GameOver는 결과 처리 흐름의 일부`라는 전제를 코드에 반영할 준비를 한다.

플레이타임/난이도 영향:
- 실패 경험 품질에 영향
- 재시작 리듬이 매끄러울수록 반복 플레이가 좋아진다.

---

## 2. 1주차 작업 목표

### 주간 목표 한 줄

`끝까지 연결될 수 있는 4스테이지 런 구조의 뼈대를 만든다.`

즉, 1주차에 콘텐츠를 많이 넣는 것이 아니라,
다음 주부터 콘텐츠를 넣을 수 있는 진입점을 만드는 것이 목적이다.

---

## 3. 1주차 작업 목록

### Task 1. 런 구조 정의

할 일:
- `RunState` 최소 구조 정의
- 포함 항목:
  - 현재 스테이지 인덱스
  - 체력
  - 폭탄
  - 로프
  - 골드
  - 액세서리

왜 필요한가:
- 다음 스테이지로 무엇을 넘길지 먼저 고정해야 이후 코드가 안 흔들린다.

임시 구현:
- 메모리 내 데이터 클래스만 추가
- 저장 시스템은 아직 붙이지 않음

최종 구현:
- RunManager가 이 상태를 소유하고 관리

직접 테스트할 것:
- 플레이 중 상태를 읽고 로그 출력 가능해야 함
- 사망/출구 진입 시 상태를 캡처할 수 있어야 함

플레이타임/난이도 영향:
- 직접 영향 없음
- 이후 자원 경제 설계의 기반

### Task 2. Stage 전환 진입점 만들기

할 일:
- 출구 진입 시 `씬 리로드` 대신 `다음 Stage 요청`을 보낼 수 있는 진입점 추가
- 아직 완전한 StageManager가 없어도 됨

왜 필요한가:
- 지금 구조에서 가장 큰 병목이 여기다.
- 이 진입점이 있어야 4스테이지 확장이 가능하다.

임시 구현:
- `GameManager.AdvanceToNextStage()` 같은 임시 메서드 호출
- 실제로는 지금 씬을 재설정하더라도 호출 구조만 먼저 바꿈

최종 구현:
- `StageManager.LoadStage(nextStage)` 로 연결

직접 테스트할 것:
- 출구 진입 시 바로 씬 리로드하지 않고 지정된 메서드가 호출되는지
- 플레이어 입력 잠금과 문 진입 연출이 유지되는지

플레이타임/난이도 영향:
- 4스테이지 러닝타임 구조의 시작점

### Task 3. GameManager 책임 분리 준비

할 일:
- 게임 루프 유지
- 대신 레벨 초기화/런 시작/플레이어 스폰 호출을 메서드 경계로 더 분명히 나눔

왜 필요한가:
- 현재 구조는 확장할수록 GameManager가 비대해질 가능성이 높다.

임시 구현:
- 파일 분할 없이 메서드 정리부터 수행

최종 구현:
- `GameLoopManager`, `RunManager`, `StageManager` 로 분리

직접 테스트할 것:
- 기존 Game 씬이 그대로 플레이 가능해야 함
- 초기화 순서가 깨지지 않아야 함

플레이타임/난이도 영향:
- 직접 영향 없음
- 개발 속도와 안정성에 큰 영향

### Task 4. Stage 데이터 껍데기 만들기

할 일:
- `StageDefinition` 또는 이에 준하는 최소 데이터 구조 생성
- Stage 이름, 룸 풀, 목표 시간만 있어도 충분

왜 필요한가:
- Stage 1-4를 코드 하드코딩 대신 데이터로 분리하기 시작해야 한다.

임시 구현:
- ScriptableObject 최소판 또는 단순 serializable class

최종 구현:
- 음악, 스폰 테이블, 보상 규칙까지 포함한 Stage 데이터

직접 테스트할 것:
- Stage 1과 Stage 2가 서로 다른 룸 배열을 읽을 수 있는지

플레이타임/난이도 영향:
- 스테이지 길이와 개성을 분리할 수 있게 됨

### Task 5. HUD 결합도 낮추기

할 일:
- `PlayerUI`의 `GameObject.Find()` 제거 계획 수립 또는 일부 치환

왜 필요한가:
- 씬 구조가 조금만 바뀌어도 UI가 깨질 수 있다.

임시 구현:
- 인스펙터 할당 필드로 옮길 수 있는 것부터 이동

최종 구현:
- UIManager가 플레이어 상태를 바인딩

직접 테스트할 것:
- 체력/폭탄/로프/골드 표시가 기존처럼 정상 갱신되는지

플레이타임/난이도 영향:
- HUD 가독성은 체감 난이도에 직접 연결됨

---

## 4. 1주차 완료 조건

이 중 최소한 아래가 되면 1주차 성공이다.

- `RunState` 최소 구조가 존재한다.
- 출구 진입이 더 이상 직접 씬 리로드를 책임지지 않는다.
- GameManager에서 런 시작/스테이지 시작의 경계가 이전보다 선명해진다.
- Stage 데이터를 넣을 수 있는 최소 진입점이 생긴다.
- 기존 Game 씬 플레이가 망가지지 않는다.

---

## 5. 1주차에 반드시 확인해야 할 버그

- 문 진입 후 입력이 풀리거나 중복 호출됨
- 플레이어 스폰이 2번 일어남
- 카메라가 새 플레이어를 따라가지 않음
- UI 텍스트 참조가 끊어짐
- 상태 캡처 시 폭탄/로프/체력 값이 실제와 다름
- 기존 테스트 씬이 동작하지 않음

---

## 6. 1주차에 포기해도 되는 욕심 요소

- Stage 2-4 완성도 높은 룸 디자인
- 상점 시스템
- 보스전
- 결과 화면 풀 연출
- 세이브 시스템
- 사운드 연출 디테일
- 완전한 UIManager 구현

---

## 7. 주간 결론

1주차는 게임을 더 재밌게 만드는 주가 아니라,
`지금 있는 재미를 4스테이지 게임으로 확장할 수 있게 구조를 여는 주`다.

가장 중요한 건 새 콘텐츠 추가가 아니라,
`씬 리로드 기반 구조`를 `런/스테이지 전환 구조`로 옮겨갈 첫 발판을 만드는 것이다.
